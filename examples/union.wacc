begin
  type intOrChar = (int | char);
  type intOrCharOrString = (intOrChar | string);

  let a : intOrChar = 'a';

  a = 4;
  a = 'c';

  let b : intOrCharOrString = a;
  b = "Hello";

  println a;
  println b;

  switch type(a)
  case int:
    print a + 5
  case char:
    print ord(a)
  end

end


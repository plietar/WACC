begin
  type foo = struct {
    int x;
  };

  type bar = struct {
    foo a;
  };

  void f(bar a) is
    print a.a.x
  end

  let a = struct {
    x = 8
  };

  let b = struct {
    a = a,
    z = 8
  };


  print b.a.x;
  a.x = 5;
  print a.x
end

